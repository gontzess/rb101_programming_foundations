# What does shift do in the following code? How can we find out?

hash = { a: 'ant', b: 'bear' }
hash.shift

# removes the key/value pair that was first originally added to the hash, and returns the pair in an array 
